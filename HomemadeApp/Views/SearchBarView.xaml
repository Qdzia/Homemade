<UserControl x:Class="HomemadeApp.Views.SearchBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HomemadeApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="800">
    <Grid>
        <Border
                Grid.Column="1"
                Margin="15" 
                Padding="5" 
                CornerRadius="18"
                Background="SeaShell">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="auto"/>
                </Grid.ColumnDefinitions>

                <Image Grid.Column="0" Source="{StaticResource SearchBarIcon}"/>

                <TextBlock x:Name="SearchText" Grid.Column="1" Margin="10"/>
                <Image Grid.Column="3" Source="{StaticResource CloseIcon}"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
