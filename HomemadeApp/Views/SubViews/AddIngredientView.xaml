<UserControl x:Class="HomemadeApp.Views.AddIngredientView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HomemadeApp.Views"
             mc:Ignorable="d" Background="White"
             d:DesignHeight="500" d:DesignWidth="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <Label Content="USAD DataBase: " Grid.Row="0" HorizontalAlignment="Center" FontSize="25"/>
        <Label Content="Find Your New ingredients Here: " Grid.Row="1" Style="{StaticResource FormLabelStyle}" />

        <ListBox Grid.Row="2" Margin="10"/>

        <TextBox x:Name="AddIngName" Grid.Row="3"  Text="NAME" Margin="10" Style="{StaticResource FormTextBoxStyle}"/>
        <TextBlock x:Name="IngData" Grid.Row="4"  Text="DATA" Margin="10"/>

        <Button Grid.Row="5" Content="Add To Your DataBase"  Style="{StaticResource NormalButtonStyle}"/>
    </Grid>
</UserControl>
