<UserControl x:Class="HomemadeApp.Views.FreeSearchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HomemadeApp.Views"
             mc:Ignorable="d" Background="DarkRed"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="5*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Column="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="5*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="SearchText" Grid.Column="1" Background="SeaShell" Margin="10"/>
            <Button Grid.Column="0" Margin="10"/>
            <Button x:Name="ChangeRecToIng" Grid.Column="2" Margin="10"/>

        </Grid>

        <ContentControl x:Name="TagBar" Grid.Row="1"/>

        <ContentControl x:Name="SearchList" Grid.Row="2"/>
        
    </Grid>
</UserControl>
